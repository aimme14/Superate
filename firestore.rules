rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas básicas - ajustar según necesidades de seguridad
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}



